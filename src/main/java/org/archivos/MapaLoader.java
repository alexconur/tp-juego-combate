package org.archivos;

import org.modelo.tablero.Tablero;
import org.modelo.tablero.FabricaCasillas;
import org.modelo.tablero.Casilla;

import java.util.List;

// *A* cuestionable
public final class MapaLoader {
  // public static Tablero construirDesde(String mapaResourcePath) {
  //   List<List<String>> t = CsvReader.readResource(mapaResourcePath);
  //   int filas = t.size(), cols = t.get(0).size();
  //   for (var fila : t) if (fila.size() != cols) throw new IllegalArgumentException("Mapa no rectangular");

  //   var casillas = new Casilla[filas][cols];
  //   for (int i = 0; i < filas; i++) {
  //     for (int j = 0; j < cols; j++) {
  //       String codigo = t.get(i).get(j);
  //       casillas[i][j] = 
  //     }
  //   }
  //   return new Tablero(filas, cols);
  // }
}
